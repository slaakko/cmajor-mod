using System;
using System.Collections;

void main()
{
    Set<int> s;
    s.Insert(1);
    s.Insert(2);
    s.Insert(3);
    for (int x : s)
    {
        if (x == 2) continue;
        Console.Out() << x << endl();
    }
}

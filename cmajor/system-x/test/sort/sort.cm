using System;
using System.Collections;

void main()
{
    List<int> numbers;
    for (int i = 0; i < 100; ++i)
    {
        int x = Abs(cast<int>(Random()));
        numbers.Add(x);
    }
    Sort(numbers);
    for (int i = 0; i < numbers.Count(); ++i)
    {
        Console.Out() << numbers[i] << endl();
    }
}

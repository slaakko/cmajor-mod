using System;

void short_mod()
{
    short left = -1;
    short right = -1;
    short result = left % right;
    RtmPrint(result);

    short x = cast<short>(RtmEvaluateBinOp(left, right, 0, 4));
    RtmPrint(x);
}

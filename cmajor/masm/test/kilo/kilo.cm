using System;

void main()
{
    string kilo;
    for (int i = 0; i < 1024; ++i)
    {
        kilo.Append(cast<char>(cast<int>('0') + (i % 10)));
    }
    auto result = kilo.RFind("901", 1023);
    Console.WriteLine(result);
}

using System;
using System.Collections;

unit_test void distance_bi_iter_test()
{
    LinkedList<int> l;
    for (int i = 0; i < 8; ++i)
    {
        l.Add(i);
    }
    long d = Distance(l.Begin(), l.End());
    #assert (d == 8);
}

using System;
using System.Collections;

unit_test void next_bi_iter_test()
{
    LinkedList<int> l;
    for (int i = 0; i < 8; ++i)
    {
        l.Add(i);
    }
    auto it = Next(l.Begin(), 4);
    #assert (Distance(l.Begin(), it) == 4);
}

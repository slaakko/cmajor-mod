using System;
using System.Collections;

void main()
{
    LinkedList<int> list;
    list.Add(1);
    list.Add(2);
    list.Add(3);
    for (auto x : list)
    {
        Console.WriteLine(x);
    }
}

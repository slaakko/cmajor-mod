export module cmajor.masm.intermediate.parser.rules;

import std.core;

export namespace cmajor::masm::intermediate::parser::rules {

std::map<int64_t, std::string>* GetRuleNameMapPtr();

} // cmajor::masm::intermediate::parser::rules
